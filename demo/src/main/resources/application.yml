server:
  port: 3030
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:5555/eureka
spring:
  application:
    name: UService
